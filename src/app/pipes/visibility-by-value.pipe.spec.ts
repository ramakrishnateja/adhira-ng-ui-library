import { VisibilityByValuePipe } from './visibility-by-value.pipe';

describe('VisibilityByValuePipe', () => {
  it('create an instance', () => {
    const pipe = new VisibilityByValuePipe();
    expect(pipe).toBeTruthy();
  });
});

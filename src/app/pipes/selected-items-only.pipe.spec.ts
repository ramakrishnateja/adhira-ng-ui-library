import { SelectedItemsOnlyPipe } from './selected-items-only.pipe';

describe('SelectedItemsOnlyPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedItemsOnlyPipe();
    expect(pipe).toBeTruthy();
  });
});
